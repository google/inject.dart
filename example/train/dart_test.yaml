# TODO(https://github.com/dart-lang/test/issues/772): Headless chrome timeout.
presets:
  travis:
    override_platforms:
      chrome:
        settings:
          headless: false

platforms:
  - chrome
